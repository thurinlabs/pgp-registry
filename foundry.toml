[profile.default]
src = "."
test = "."
out = "out"
script = "script"
solc = "0.8.24"

fs_permissions = [{ access = "read", path = "script/" }]

[lint]
ignore = ["lib/**/*.sol"]

[rpc_endpoints]
sepolia = "${SEPOLIA_RPC_URL}"
