[profile.default]
src = "."
test = "."
out = "out"
script = "script"
solc = "0.8.24"
via_ir = true
optimizer = true
optimizer_runs = 200

fs_permissions = [{ access = "read", path = "script/" }]

[lint]
ignore = ["lib/**/*.sol"]

[rpc_endpoints]
sepolia = "${SEPOLIA_RPC_URL}"
